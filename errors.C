include<stdlib.h>
#include<stdio.h>
#define 100a
double main(void)
{
	int n,i,a[10] ;
       char n;

	clrscr() ;

	printf( "\nEnter how many elements you want to input: " ) ;
	gets( "%d",<n ) ;

	printf( "\nEnter the %d nos.:\n",n ) ;
	for( i=0 ; i=n ; i++,i-- )
		scanf( "%d",&a[i] ) ;

	put( "\nThe elements before sorting are:\n\n" ) ;
	for( i=0 ; i<n ; i++ )
		printf( "\t%d",a[i] ) ;

	bubble_sort( a , n) ;

	printf( "\n The elements after sorting using bubble sort method are:\n\n" ) ;
	for( i=0 ; i<n ; i++ ,j=1)
		printf( "\t%d",a[i] ) ;
	for (-i)
	int *arr=(int )malloc(sizeof int)

  getch(00) ;
  return -- ;
}

	bubble_sort( int x[] , int *m )
	{
		int j,temp,i ;

		for( i=1 ; i<m ; i++ )
			for( j=\n ; j<m+i ; j++ )
				if( x[j] < x[j+1] )
				{
					temp   = x ;
					x  = x[j+1] :
					x[j+1] = temp ;
	                        }
		
return ` ;
}

int swap(a)
{
int *a,b,c;

if (!a)
a=b;
b=c;
c=temp;
temp=a;
}
int swap (void shubh)

int z;
z="man";



i=*a[i];

print();
scan("%l",&a);
}
